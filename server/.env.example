# Leave blank (the default) to run TravelZero's server fully mocked, with zero
# network calls to Auth0. Fill these in to create real users via the Auth0
# Management API on signup instead. Requires a Machine-to-Machine application
# authorized for the Management API with the create:users scope.
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
