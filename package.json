{
  "name": "travelzero",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client,caddy -c blue,magenta,green --kill-others-on-fail \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"sudo caddy run --config client/Caddyfile\"",
    "dev:fresh": "rm -rf client/.vite && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
