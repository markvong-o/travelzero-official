{
  "name": "travelzero",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"CADDY_HOST=travel0.idzero.mvbuilt.com npm run dev --prefix client\"",
    "dev:fresh": "rm -rf client/.vite && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
