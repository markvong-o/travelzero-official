# Leave blank (the default) to run TravelZero fully mocked, with zero network
# calls to Auth0. Fill these in to authenticate against a real Auth0 tenant instead.
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=
